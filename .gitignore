# 密钥与本地环境 —— 绝不入库
.env

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# 编辑器 —— IDE 配置含本机绝对路径，不入库
.idea/
.vscode/

# 运行产物 —— 会话日志与本地记忆库仅供本机使用，不入库
chat.log
*.db

# 检索产物 —— 索引能从笔记重建、模型能重新下载，都不入库
search-index.json
models/
